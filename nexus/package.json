{
  "name": "nexus-program-command",
  "version": "1.0.0",
  "description": "NEXUS — program management command app for Security App Factory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:client": "cd client && npm run build",
    "start:prod": "npm run build:client && node server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3"
  }
}
