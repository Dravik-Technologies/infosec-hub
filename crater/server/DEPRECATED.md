# DEPRECATED

This directory is a legacy prototype and is **no longer maintained**.

The active backend is in [`../backend/`](../backend/).

Do not add new features or run this code — use `../backend` instead.
